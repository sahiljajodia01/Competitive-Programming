#include <bits/stdc++.h>

using namespace std;

void allStringOfKLength(string set[], int k, int pos) {
    if(pos > k) {
        return;
    }
    else {
        for()
    }
}

int main() {
    string set[] = {'a', 'b'};
    int k;

    cout << "Give the value of k: ";
    cin >> k;

    allStringOfKLength(set, k, 0)
}