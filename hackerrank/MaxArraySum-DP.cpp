//////////////////// This problem is not solved yet. I have to first solve basic problems of DP.


#include <bits/stdc++.h>

using namespace std;

int max_array_sum(vector<int> v, int n, int sum) {
	if(n == 0 || n == 1) {
		sum = 0;
		return sum;
	}
	else {
		for(int i = n-1; i >=0; j -= 2) {

		}
	}

}

int main() {
	int n, ans, sum = 0;

	cin >> n;

	for(int i = 0; i < b; i++) {
		int temp;
		cin >> temp;
		v.push_back(temp);
	}

	ans = max_array_sum(v, n, sum);

	cout << ans << "\n";

	return 0;
}