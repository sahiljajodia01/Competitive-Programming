// PAYING - Paying in Byteland
#include <bits/stdc++.h>

int minimumCoins(int sum) {
	vector<int> arrayOfCoins = {};
	for(int i = 0; i <= sum; i++) {
		if(for j = 1; j <= i; j++) {
			
		}
	}
}


int main()
{
	int t;

	cin >> t;
	for(int i = 0; i < t; i++) {
		int sumOfCoins;
		cin >> sumOfCoins;
		int coins = minimumCoins(sumOfCoins);
		cout << coins << "\n";
	}
}